name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deployment notes
        run: |
          echo "Add your target platform secrets, then replace this placeholder with deployment commands."
          echo "Backend entrypoint: backend/app.py"
          echo "Frontend build command: npm run build in frontend/"
