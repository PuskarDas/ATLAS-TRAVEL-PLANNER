# ML Model Details

The starter ML pipeline uses a synthetic travel preference dataset and a Random Forest regression baseline.

Metrics produced by `ml_training/scripts/train_recommendation_model.py`:

- MAE
- RMSE
- R2

The notebooks in `ml_training/notebooks/` wrap the scripts so you can show preprocessing, training, and evaluation in a Jupyter workflow.
